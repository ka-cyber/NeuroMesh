# Legacy plain-pip install (prefer `pip install -e ".[all]"` via pyproject.toml).
# Kept in sync manually -- pyproject.toml is the source of truth for dependency bounds.
torch>=2.0
numpy>=1.24
scipy>=1.10           # HD95 / region-metric computation (neuromesh.evaluation.metrics)
nibabel>=5.0
h5py>=3.8
matplotlib>=3.7
pytest>=7.4
# monai is OPTIONAL -- one test (test_dice_loss_matches_monai_within_tolerance)
# cross-checks our Dice implementation against it and is auto-skipped if absent.
# monai>=1.6
