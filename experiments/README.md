# Experiment manifests

`manifests/` contains one JSON per training run actually performed, recording exactly what was run and in what order. See `docs/experiments.md` for the narrative version and `PROTOCOL_FREEZE_v1.md` for the frozen protocol these all share.
